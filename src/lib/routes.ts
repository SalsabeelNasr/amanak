/** Pathnames without locale prefix — use with `Link` from `@/i18n/navigation`. */
export const ROUTES = {
  leadEntry: "/estimate",
  inquiry: "/inquiry",
  treatments: "/treatments",
  hospitals: "/hospitals",
} as const;
